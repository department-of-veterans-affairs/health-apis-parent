package gov.va.api.health.sentinel.categories;

/**
 * JUnit test category for tests that exercise functionality that is not publically available. For
 * example, internal-only APIs might be exercised through internal paths, but excluded when testing
 * the application from publicly available paths.
 */
public interface InternalApi {}
